all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  vars:
    app_name: locatic-app
    namespace: devops-training
    kube_context: minikube
    terraform_dir: "../terraform"
    app_port: 3000
